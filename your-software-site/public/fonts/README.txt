Place your font files here.

Expected filenames (update if your files differ):
- Maxbeda-Regular.woff2
- Maxbeda-Regular.woff

After adding the files, the site will load the display font via the @font-face in styles/globals.css.