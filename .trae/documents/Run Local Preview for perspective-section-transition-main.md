## Target Project

* Preview `perspective-section-transition-main` (you opened a file in this project)

* Alternative: Also can preview `awwwards-side-menu-main` on request

## Prerequisites

* Use `npm` (project has `package-lock.json`)

* Node 18+ recommended for Next.js 13/14

* No `.env` or custom `PORT` found; defaults to `http://localhost:3000`

## Dev Preview Steps

* Navigate to `perspective-section-transition-main`

* Install deps: `npm ci` (fallback `npm install` if `ci` fails)

* Start dev server: `npm run dev`

* Open `http://localhost:3000/` and verify page renders

* If port conflict: `npm run dev -- -p 3001`, then open `http://localhost:3001/`

## Production Preview (optional)

* Build: `npm run build`

* Serve: `npm run start`

* Open `http://localhost:3000/` (or `-p 3001` if needed)

## Deliverables

* Launch the server and provide a live preview URL

* Confirm successful load and report any runtime errors with fixes

## Next

* On your approval, I will start the dev server and share the preview URL immediately.

